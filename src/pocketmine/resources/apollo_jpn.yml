player:
  # 有効にするとプレイヤーが死亡してもインベントリ(プレイヤーの持っている、ブロックやアイテム)を維持します
  keep-inventory: false
  # 有効にするとプレイヤーが死亡しても経験値を維持します
  keep-experience: false

dimensions:
  # 有効にするとネザーワールドが自動的に生成され、ポータルを用いて移動できます
  nether:
    active: true
    level-name: "nether"
  # 有効にするとエンドワールドが自動的に生成され、ポータルを用いて移動できます
  end:
    active: true
    level-name: "end"

developer:
  # 有効にするとソースディレクトリからプラグインを読み込みます
  folder-plugin-loader: true
  # 有効にすると開発者向けのコマンドを有効にします
  commands: true
  # 有効にすると互換性のないAPIのプラグインを読み込みます
  load-incompatible-api: true

level:
  # 有効にすると、mobsは動き回ることができます
  # 注：この機能は、多数のmobが生成されたときにサーバーの遅延を引き起こす可能性があります
  enable-mob-ai: true
  # Generic Auto Mob Spawning, enable if you want spawn mobs in your world
  generic-auto-mob-spawning: true
